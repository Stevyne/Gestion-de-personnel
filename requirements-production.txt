# Alias explicite pour les plateformes qui utilisent ce nom.
# requirements.txt contient désormais toutes les dépendances runtime, y compris
# Flask-Migrate, Redis, S3 et Sentry, afin que les anciens services Render dont
# la commande est `pip install -r requirements.txt` démarrent correctement.
-r requirements.txt

diff --git a/templates/base.html b/templates/base.html
index b3362fa..c80a3b9 100644
--- a/templates/base.html
+++ b/templates/base.html
@@ -454,18 +454,22 @@
         }
 
         // Full event isolation for drawer contents
+        // NOTE: stopPropagation() (not stopImmediatePropagation()) — on garde
+        // le blocage de la remontée vers l'overlay/document, mais on ne coupe
+        // pas les AUTRES écouteurs du même élément (ex: la fermeture auto au clic
+        // sur un lien, ajoutée plus tard par setupAutoClose()).
         function guardEvents(el) {
             if (!el) return;
             ['click','touchstart','touchend','pointerdown'].forEach(t => {
-                el.addEventListener(t, e => e.stopImmediatePropagation(), { passive: false });
+                el.addEventListener(t, e => e.stopPropagation(), { passive: false });
             });
         }
         if (navDrawer) {
             guardEvents(navDrawer);
             ['.drawer-header','.drawer-links','.drawer-user-section'].forEach(s => guardEvents(navDrawer.querySelector(s)));
             navDrawer.querySelectorAll('a,button,.nav-link,.drawer-logout').forEach(el => {
-                el.addEventListener('click', e => e.stopImmediatePropagation(), { passive: false });
-                el.addEventListener('touchstart', e => e.stopImmediatePropagation(), { passive: false });
+                el.addEventListener('click', e => e.stopPropagation(), { passive: false });
+                el.addEventListener('touchstart', e => e.stopPropagation(), { passive: false });
             });
         }
 
